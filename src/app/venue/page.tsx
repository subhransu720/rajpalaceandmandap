import VenueContent from './VenueContent';

export const metadata = {
  title: 'Our Venue | Raj Palace & Convention',
  description: 'Explore our luxurious venue facilities including halls, lawns, rooms, and amenities at Raj Palace & Convention.',
};

export default function VenuePage() {
  return <VenueContent />;
} 